import { oneStepMore } from 'brain/helpers/one-step-more';
import { module, test } from 'qunit';

module('Unit | Helper | one step more');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = oneStepMore([42]);
  assert.ok(result);
});
