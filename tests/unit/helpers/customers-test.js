import { customers } from 'brain/helpers/customers';
import { module, test } from 'qunit';

module('Unit | Helper | customers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = customers([42]);
  assert.ok(result);
});
